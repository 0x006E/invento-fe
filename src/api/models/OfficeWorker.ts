/**
 * Represents an office worker.
 * @interface
 */
export interface OfficeWorker {
  /** The unique identifier of the office worker. */
  id: string;
  /** The name of the office worker. */
  name: string;
}
