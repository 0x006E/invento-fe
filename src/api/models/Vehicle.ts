/**
 * Represents a vehicle object.
 */
export interface Vehicle {
  /**
   * Unique identifier of the vehicle.
   */
  id: string;
  /**
   * The vehicle number or registration number.
   */
  number: string;
  /**
   * The type of vehicle.
   */
  type: string;
}
